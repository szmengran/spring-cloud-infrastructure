server:
  port: 10002
spring:
  application:
    name: oauth
  profiles:
    active: ${ENVIRONMENT}
  cloud:
    config:
      uri: ${CONFIG_SERVER_ADDRESS}
      fail-fast: true
logging:
  level:
    org.springframework.security: debug